# Bloqueia qualquer acesso direto a esta pasta via navegador.
# Sintaxe compatível com Apache 2.4+ (Hostnet/XAMPP recentes) e 2.2.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
